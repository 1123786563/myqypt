---
status: accepted
---

# Retain native Product UIs behind the Platform edge

The Portal owns App Center, purchase, membership, usage, billing, and settings, while each Product retains its upstream UI behind a Higress-controlled Product route with OIDC SSO and trusted Platform Context. Stage 1 rejects iframe embedding and broad frontend Forks because unified entry and commerce do not require rewriting every Product presentation layer.
