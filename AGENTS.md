## Agent skills

### Issue tracker

Issues are tracked in GitHub repository `1123786563/myqypt`; always pass `--repo 1123786563/myqypt` because the Git remote uses a proxy URL. See `docs/agents/issue-tracker.md`.

### Triage labels

Use the canonical `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, and `wontfix` labels. See `docs/agents/triage-labels.md`.

### Domain docs

This is a single-context repository with `CONTEXT.md` at the root and ADRs under `docs/adr/`. See `docs/agents/domain.md`.
